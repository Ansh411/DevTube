import TagList from "./TagsList";
import VideoContainer from "./VideoContainer";

const MainContainer = () => {
  return (
    <div>
        <TagList/>
        <VideoContainer/>
    </div>
  );
};

export default MainContainer;